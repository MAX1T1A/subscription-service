root = "."
tmp_dir = "tmp"

[build]
  cmd = "swag init -g cmd/app/main.go -o docs && go build -buildvcs=false -o ./tmp/app ./cmd/app"
  bin = "./tmp/app"
  include_ext = ["go", "sql"]
  exclude_dir = ["tmp", "docs"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
